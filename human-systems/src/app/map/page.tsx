"use client";
/**
 * MAP (Step 6A): what seems connected. For now this is the existing
 * feedback-map capability under the simple navigation; its mathematics
 * and editing are unchanged.
 */
import FeedbackMapPage from "@/app/feedback-map/page";

export default function MapPage() {
  return <FeedbackMapPage />;
}
