# Migration 019 (PR-T) rollout and emergency runbook

STATUS: AUTHORED, NOT RUN. Migration 019 exists as a generated, test-executed
artifact on a local branch. Nothing in this document is authorization: the
code release, the preflight, the migration execution, the post-install
verification and any rollback are each a SEPARATE, explicit Andres
authorization in Claude's chat, quoting the checksum recorded below.

## What 019 changes, in one paragraph

`accept_quote_create` and `accept_quote_edit` are replaced IN PLACE (identical
signatures — no PostgreSQL overload; ACLs, ownership, SECURITY DEFINER and
search_path preserved and verified) with the migration-018 bodies plus the
PR-T pickup guard, and one helper is added:
`public.linkmia_pickup_is_future(TIMESTAMPTZ, TIMESTAMPTZ)` — `LANGUAGE sql
IMMUTABLE STRICT`, security invoker, owned by the captured writer owner,
EXECUTE revoked from PUBLIC, anon, authenticated and service_role. After it
runs, every genuinely NEW write requires the submitted (create) or resolved
proposed (edit) pickup instant to be strictly later than the database clock:
once before mutation on `transaction_timestamp()`, and again as the LAST
potentially blocking statement of the inner subtransaction on
`clock_timestamp()` (SQLSTATE `ZQ019` → booking, acceptance, receipt AND
telemetry roll back → outcome `pickup_time_elapsed`). Receipt-first replay is
untouched: an exact retry lands on its receipt before any clock is read. No
table, column, index, constraint, trigger or RLS change, and NO grant change to
the existing writers (identity/owner/ACL/config verified equal to the
pre-capture). The one ACL event is the NEW helper, which gets a RESTRICTED ACL:
EXECUTE for the captured writer owner(s) only — PUBLIC, anon, authenticated,
service_role and every other grantee revoked, the exact set verified. The
`quote_verifications` verdict CHECK is untouched (the guard writes no verdict
row; the edit writer's pre-existing operationId-less `no_request_id`
instrument is unchanged and pinned — see Known residuals); no minimum lead
time; fares, tokens, cancellation and driver-status semantics unchanged;
`accept_optional_edit` untouched.

The artifacts are GENERATED by `database/migrations/tools/prt-guard-transform.js`
and must never be hand-edited: `node database/migrations/tools/prt-guard-transform.js --check`
and `tests/prt-pickup-integrity.test.js` assert byte-equality.

## Ordering relative to the code deploy — CODE FIRST, then SQL

The PR-T code (endpoint mapping `pickup_time_elapsed` → definitive HTTP 400,
the quote endpoint's staged strict-future gates, the create-side passenger
message, cache rung `linkmia-v1.3.28` / `linkmia-runtime-v5`) is deployed and
verified BEFORE the migration:

- code first, SQL later: the old writers never return `pickup_time_elapsed`,
  so the new mapping is inert until 019 installs; the quote endpoint's gates
  already refuse elapsed intents on the server clock; nothing breaks.
- SQL first, code later is FORBIDDEN: an old handler would map the new
  outcome through `unknownOutcomeResponse` as a sanitized 500 — a real
  refusal presented as an outage.

Between the code deploy and the migration the phase-bounded PR-T
pre-migration code rollback rung (`linkmia-v1.3.29` / `linkmia-runtime-v6`)
exists: a code revert commit taking that fresh pair. It is RETIRED once the
migration is verified — after that, removing the endpoint mapping, the
passenger handling or the helper dependency while guard-carrying writers are
live is a separately designed coordinated SQL-and-code decision.

## Before the window

1. Confirm the reviewed commit. Re-run the full suite in three timezones,
   `node tests/prt-pickup-integrity.test.js` (real PostgreSQL: artifact
   byte-equality; static scope pins; comparator boundary incl. the `>`→`>=`
   mutant; the executed authority MATRIX `{off, observe} × {verified,
   no_token, verify_failed}` for BOTH writers plus `enforce × verified`,
   observe/enforce reached only through `set_pricing_mode`; the edit
   RESCUE; lifecycle/version conflicts winning over elapsed time; the final
   live-clock guard rolling back booking, ACCEPTANCE, receipt and telemetry
   for verified and non-token paths, each verified crossing paired with an
   acceptance-path control; receipt-first replay per token kind; executed
   guard mutants; the rollback on THREE baselines with the exact helper
   ACL, the rogue-grantee/PUBLIC probe, the canonicalization-less
   transform refusing to commit, and the helper-less rollback MUTANT that
   installs a runtime outage; and THIS preflight executed on the 018
   baseline and after 019), `node tests/r1-migration-chain.test.js`, and
   `node tests/r1-route-content.test.js`.
2. `node database/migrations/tools/prt-guard-transform.js --check` → both
   artifacts match. Then compute and record `shasum -a 256` of
   `database/migrations/019_prt_pickup_time_integrity.sql` AND
   `database/migrations/018_r1_rollback.sql`. Andres's execution
   authorization must quote the 019 hash; the pasted bytes must match.
3. Verify the PR-T CODE is live: served `service-worker.js` carries
   `linkmia-v1.3.28` / `linkmia-runtime-v5`; served `indexMVP.html` contains
   the `pickup_time_elapsed` create-side branch and the copy
   "This pickup time has passed. Choose a new time to continue."; an
   anonymous `POST /api/quote-ride` still answers 401 (secret config held).
4. Read-only preflight in the Supabase SQL editor, ONE GRID EACH, from
   `database/migrations/019_prt_preflight.sql`:
   - A1: exactly the two writers, SECURITY DEFINER, search_path present,
     `sr_exec=true`, client execs false.
   - A2: two rows, each `is_018=true` (row-specific marker) and
     `guarded=false`.
   - A3: helper signatures = 0.
   - B1: `mode IN ('off','observe')`, `enforcement_started_at IS NULL`.
     Anything else: STOP.
   - B2: exactly ONE row, `quote_verifications_verdict_check`, 16 verdict
     literals, `widened=false`.
   - C1/C2: record the activity snapshot and the row counts.
5. Take a Supabase restore/PITR marker; record the catalog and privilege
   evidence beside the preflight grids and the hashes.

## The window

1. Under its own explicit authorization, set `WATCHDOG_DISABLED=1` and
   redeploy; confirm the disabled tick. Announce a quiet window; close known
   passenger/driver tabs; do not proceed while C1 shows a writer call in
   flight. Existing future nonterminal bookings are expected and untouched.
2. Recompute the 019 hash and match it to the authorization. Paste those
   exact bytes into the SQL editor and run them ONCE. One transaction, four
   built-in gates, each aborting everything on RAISE:
   - PRE-CAPTURE of both writers' identities/owners/ACLs, refusing privilege
     drift;
   - the HELPER, created/replaced and assigned from the captured owner with
     its EXECUTE grantees canonicalized to exactly the captured writer
     owner(s) — every other grantee, PUBLIC included, revoked;
   - VERIFICATION: one function per name, identity/owner/ACL/config equal to
     the capture, SECURITY DEFINER + search_path, privilege ceiling proven
     behaviorally, BOTH guards present on the right clocks in SEQUENCE (a
     presence tripwire — the EXACT order, final guard after the last
     success-path telemetry insert, is pinned by the executed suite's
     STATEMENT ORDER row, not by this LIKE check), the ZQ019 handler
     present, `pickup_time_elapsed` never a verdict, 018's NULL-duration
     markers still present, the helper's signature/volatility/strictness/
     owner/ACL and its exact boundary;
   - a ROLLBACK-CONTAINED SMOKE (sentinel `ZZ019`): an elapsed no-token
     create refused with zero writes, a future create succeeds, an elapsed
     edit proposal refused with the row untouched, a future edit succeeds —
     then forced rollback and a row-count residue proof (no sequence rewind,
     018 precedent).
   Only `Success. No rows returned` is a pass. Any error rolled back the
   whole transaction: diagnose, return to preflight, never run fragments.
3. POST-INSTALL VERIFICATION, read-only, one grid each:
   - A1 again: identical identities, `sr_exec=true`, client execs false.
   - A2 again: `guarded=true` for both rows, `is_018` still true.
   - A3 again: helper signatures = 1; and
     ```sql
     SELECT p.provolatile, p.proisstrict, p.prosecdef, pg_get_userbyid(p.proowner) AS owner,
            has_function_privilege('anon', p.oid, 'EXECUTE') AS anon_x,
            has_function_privilege('authenticated', p.oid, 'EXECUTE') AS auth_x,
            has_function_privilege('service_role', p.oid, 'EXECUTE') AS sr_x
     FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
     WHERE n.nspname = 'public' AND p.proname = 'linkmia_pickup_is_future';
     ```
     Expect `i`, true, false, the writer owner, and three `false`; and
     ```sql
     SELECT pg_get_userbyid((x.a).grantee) AS grantee, (x.a).privilege_type
     FROM (SELECT aclexplode(p.proacl) AS a FROM pg_proc p
           WHERE p.oid = 'public.linkmia_pickup_is_future(timestamptz,timestamptz)'::regprocedure) x;
     ```
     Expect ONLY the writer owner(s), each with EXECUTE — no PUBLIC, no other
     role.
   - B1 again: mode unchanged, `enforcement_started_at` still NULL.
   - C2 again: row counts identical to preflight (zero smoke residue).
4. Re-enable the watchdog (`WATCHDOG_DISABLED` unset + redeploy) under its
   own authorization; confirm the next tick.
5. RETIRE the pre-migration code rollback rung (record it as retired in the
   rollout record). From here the emergency path is the sequenced rollback
   below, which keeps the guard.

## Live smoke (production, no paid calls beyond the normal flow)

1. From the signed-in test account, attempt a NEW booking whose pickup time
   is deliberately in the past (the picker snaps forward, so this is done
   by holding the When step open across the chosen minute, then tapping
   Book): expect the typed message on the When step, focus on the time
   control, every entered value preserved, NO booking row, NO provisional
   `trip_` record, and the envelope settled (no recovery card).
2. Create one real test booking >24h out: expect success.
3. Edit that booking (pending edit) to a time already passed: expect the
   same typed refusal and an unchanged row; then to a future time: expect
   success with `details_version` incremented.
4. Cancel it by the usual protocol (CANCEL, never DELETE).
5. Confirm `quote_verifications` gained NO row for the refusals (compare
   C2 before/after, allowing only the successful create/edit rows). This
   holds for the browser, which always sends an operationId; an
   operationId-less legacy edit would add its pre-existing `no_request_id`
   row (Known residuals).

## Emergency rollback — sequenced (SQL-only rollback is incompatible with the R1 code)

The standing emergency artifact is `database/migrations/018_r1_rollback.sql`,
REGENERATED by PR-T: it restores the 017 bodies WITH the pickup guard and is
SELF-CONTAINED — it creates/replaces the helper first, so it is valid whether
019 was installed or not (a rollback that assumed the helper would succeed at
CREATE and then fail every booking at RUNTIME; `tests/prt-pickup-integrity.test.js`
executes that mutant to prove the class). The sequence, each step separately
and explicitly authorized, is `docs/R1-MIGRATION-RUNBOOK.md`'s: HOLD DARK →
CODE FIRST (fresh cache rung, drain in-flight requests) → SQL SECOND (this
checksum-matched artifact) → post-rollback verification. Its rung facts are
the plan v8.6 §3D ladder: browser-flag rollback `v1.3.32`/`v9`, emergency R1
revert `v1.3.33`/`v10`.

## Known residuals (recorded 2026-09-09, accepted for this release)

- **Old-tab raw token, one session.** A booking page cached under the
  previous worker (`linkmia-v1.3.27`) keeps its generic quote-error branch
  until the `v1.3.28` worker activates on the following page load. The new quote
  endpoint answers an elapsed pickup with `{ error: 'pickup_time_elapsed',
  message }`, and that older page renders the `error` token verbatim in the
  quote card — where the previous endpoint sent the sentence "pickupAt is in
  the past" for pickups more than five minutes past (and still quoted the
  ±5-minute window). One-release transition residual; no data or money
  effect; clears on reload.
- **operationId-less edit telemetry (pinned, not moved).** In
  `accept_quote_edit` the inherited 017 `no_request_id` telemetry INSERT for
  a caller with no operationId (off/observe only) runs BEFORE the pre-guard
  and outside the subtransaction, so such a caller's elapsed refusal — or a
  final-guard crossing — leaves exactly that one telemetry row (no booking,
  acceptance or receipt). The create writer's counterpart sits inside the
  guarded block and leaves nothing. The browser always sends an operationId.
  `tests/prt-pickup-integrity.test.js` pins the positions in both artifacts
  and executes the lane in off and observe; moving the insert is a separate,
  deliberate 017-instrument decision.

## What this runbook does NOT cover

- Any change to fares, tokens, TTLs, cancellation, driver statuses, or a
  minimum lead time — none exists and none is introduced.
- The parked PR-B review card: it releases AFTER this migration is verified,
  under its own authorization, on its own rung (`v1.3.30`/`v7`).
- Historical rows: an already-stored elapsed pickup is not rewritten; it is
  refused only when a NEW write proposes an elapsed instant, and a pending
  ride whose old time passed can be rescued by submitting a future time.
